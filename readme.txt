API 

first npm install package.json ans pip install requirements.txt

******************************************************

POST http://localhost:3000/crawlReviews

return the reviews of nearest places

require

{
    "name":"name_of_the_place"
}

*******************************************************

POST http://localhost:3000/crawlNearestPlaces

return the nearest places 

require

{
    "name":"name_of_the_place"
}

// "test": "echo \"Error: no test specified\" && exit 1",